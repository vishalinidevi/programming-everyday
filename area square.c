Find the area of a square given the length of a side.
#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,area;
    printf("Enter th length of the side: ");
    scanf("%d",&a);
    area=a*a;
    printf("Area of thee square %d",area);
    return 0;
}
